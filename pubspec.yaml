name: recipe_app
description: "Recipe App Using clean Architecture"
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  connectivity_plus: ^7.1.1
  dio: ^5.9.2
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_svg: ^2.3.0
  get_it: ^9.2.1
  go_router: ^17.2.3
  shimmer: ^3.0.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
